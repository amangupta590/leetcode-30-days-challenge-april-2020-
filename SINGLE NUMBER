class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int final=0;
        for(int i=0;i<nums.size();i++){
            final=final^nums[i];
        }
        return final;
        
    }
};
